{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
    <h1>Dashboard</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dashboard.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ dashboard.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ dashboard.email }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ dashboard.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ dashboard.lastname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dashboard_index') }}">back to list</a>

    <a href="{{ path('app_dashboard_edit', {'id': dashboard.id}) }}">edit</a>

    {{ include('dashboard/_delete_form.html.twig') }}
{% endblock %}
